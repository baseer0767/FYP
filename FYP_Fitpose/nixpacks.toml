[build]
cmd = "pip install -r requirements.txt"

[phases.setup]
aptPkgs = ["libxcb1", "libx11-6", "libxext6", "libxrender1", "libsm6", "libgl1-mesa-glx"]

[start]
cmd = "uvicorn api.server:app --host 0.0.0.0 --port $PORT"